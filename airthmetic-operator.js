function getArea(length = 3, width = 4.5) {
    let area;
    area = length * width;
    console.log(area);
    return area;
}
getArea();

function getPerimeter(length = 3, width = 4.5) {
    let perimeter;
    area = 2 * (length + width);
    return perimeter;
}
getPerimeter();